# Google Places API (New). Get key from Google Cloud Console; add to Vercel env vars.
GOOGLE_PLACES_API_KEY=

# Basic auth (protects entire app). Use either:
# Option A – separate vars:
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=

# Option B – single var (overrides Option A if set):
# BASIC_AUTH_CREDENTIALS=username:password
